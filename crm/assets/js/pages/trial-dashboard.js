/* ============================================================
   MOVED. This is now pages/practice.js.
   ------------------------------------------------------------
   The dashboard was built as one of three homes and did not stay
   one. It lives at practice.html, behind the More button with the
   machinery, and its module went with it.

   IT HOLDS NO LOGIC ON PURPOSE. Two copies of one screen means the
   one nobody opens quietly stops matching the API it reads. The
   file stays — nothing here is deleted — but what it says is where
   to look.

   See pages/practice.js for the module, and the header comment in
   practice.html for why a dashboard is a monthly screen at this
   practice's scale rather than a morning one.
   ============================================================ */
