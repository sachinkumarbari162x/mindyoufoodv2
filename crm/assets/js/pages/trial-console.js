/* ============================================================
   MOVED. This is now pages/home.js.
   ------------------------------------------------------------
   The console trial won the comparison and became the CRM's home,
   so the module went with it: index.html loads pages/home.js, and
   this file loads nothing.

   IT HOLDS NO LOGIC ON PURPOSE. Keeping the old copy alongside the
   live one would give two implementations of one screen, and the
   one nobody opens is the one that quietly stops matching the API
   it reads. The file itself stays — nothing here is deleted — but
   what it says is where to look.

   See pages/home.js for the module, and the banner in
   overview-classic.html for why the comparison ended the way it
   did.
   ============================================================ */
